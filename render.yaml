services:
  - type: web
    runtime: static
    name: portal-atendimento-operacao
    buildCommand: npm ci && npm run build
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
